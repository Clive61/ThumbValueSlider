struct ContentView: View {
    @State private var currentValue : CGFloat = 15
    @State private var currentValue2 : CGFloat = 10
    @State private var currentValue3 : CGFloat = 1.5
    @State private var currentValue4 : CGFloat = 12
    var body: some View {
        VStack {
            HStack {
                VStack() {
                    //Slider(value:$currentValue2, in: 1...31, step:3)
                    ThumbValueSlider(value: $currentValue, range: 0...31 , step:3 ,  label: "xxx",  asInteger: true)
                    ThumbValueSlider(value: $currentValue2, range: 1...31, step: 3 ,textColor: Color.blue, accentColor: Color.blue
                        ,asInteger: true)
                    ThumbValueSlider(value: $currentValue3, range: 1...31 ,step: 0.5 , accentColor: Color.green, unselectedColor: Color.blue)
                    ThumbValueSlider(value: $currentValue4, range: 1...31.5 , step: 2, textColor: Color.yellow, accentColor: Color.yellow)
                }
                .padding()
                    .padding()
                    .font(.system(size: 12))
            }
            .padding(.horizontal)
        }
    }
    
}